<div class="container">

  <h1>Dashboard</h1>

  <h2>Bienvenue, <%= current_user.email %>!</h2>

  <p>Tu as transcrit <%= current_user.submitted_pages.count %> pages, dont <%= current_user.integrated_pages.count %> ont été validées et <%= current_user.submitted_pages.pending_review.count %> sont en attente de validation.</p>

  <% if current_user.has_draft_page? %>
      <p>Tu as commencé la transcription d'une page. Veux-tu <%= link_to "reprendre maintenant ?", edit_page_path(current_user.draft_page) %></p>
  <% else %>
      <p><%= link_to "Transcrire une nouvelle page", checkout_pages_path, :method => :patch %></p>
  <% end %>

  <h2>Avancement du projet</h2>

  <p><%= number_to_human(Page.percent_validated) %>% de pages validées</p>
  <p><%= Page.submitted.count %> pages transcrites</p>
  <p><%= Page.reviewed.count %> pages validées</p>

  <h2>Gestion de mon compte</h2>

  <p><%= link_to "Modifier mon mot de passe", edit_user_registration_path %></p>

</div>